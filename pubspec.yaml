name: allapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # github
  github: ^9.2.0

  # web related
  flutter_html: 3.0.0-beta.2
  url_strategy: ^0.2.0
  universal_html: ^2.0.4

  # http
  http: ^0.13.6

  # utils
  mailto: ^2.0.0
  flip_card: ^0.7.0
  url_launcher: ^6.2.6
  cupertino_icons: ^1.0.2
  carousel_slider: ^4.2.1
  animated_text_kit: ^4.2.2
  font_awesome_flutter: ^10.1.0

  # state management
  provider: ^6.0.1

  # cache
  shared_preferences: ^2.0.6
  image_gallery_saver: ^2.0.3
  share: ^2.0.4
  mailer: ^6.1.0
  path_provider: ^2.0.2
  math_expressions: ^2.0.0
  flutter_svg: ^2.0.10+1
  weather_icons: ^3.0.0
  geolocator: ^12.0.0
  folio:
    path: D:/University/Flutter Project/Portfolio-main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  http: ^0.13.6
  flip_card: ^0.7.0
